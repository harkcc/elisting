# Component Gallery

This folder contains lightweight previews for candidate components.

Current preview:

- `gif-motion-html-preview.html`: first batch of small GIF-like HTML/CSS motion components.

These previews are selection artifacts. Approved components should later receive component cards, renderer implementation, static fallback, and optional GIF/WebP export.
